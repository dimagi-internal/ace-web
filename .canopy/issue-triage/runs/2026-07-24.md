# Issue triage — dimagi-internal/ace-web — 2026-07-24

Target: `dimagi-internal/ace-web` (local working tree, branch `emdash/issue-triage-8tx6s`)
Open issues: 10; triaged 9 (skipped #673 per user — another agent is implementing it). No truncation.

## Verdicts

| # | Disposition | Conf | Effort | Title | Evidence / why | Action taken |
|---|-------------|------|--------|-------|----------------|--------------|
| 585 | close | high | — | Autonomous run execution: no headless turn-trigger | `apps/sessions/turn_driver.py:326-354` detached `drive_turn` subprocess (docstring cites #585); `apps/opps/api.py:1773` seeded-run calls it; tests in `apps/sessions/tests/test_turn_driver.py:285-301` | pending gate |
| 346 | close | high | — | fork-run alignment: run-id format + run_state.yaml filename | `apps/opps/opp_forker.py:319-335` mints `YYYYMMDD-HHMM` run-ids; `opp_forker.py:238` writes `run_state.yaml`; old `fork.py`/`state.yaml` gone | pending gate |
| 539 | close | high | — | decisions.yaml: simplify status enum + trim non-overridden rows on fork | `apps/opps/opp_forker.py:735-739` keep-overrides-only trim (PR #541); `apps/opps/decisions_edit.py:104-146` v1→v2 enum collapse to `{ai-default, overridden}`; PR #575 run_state-based step status | pending gate |
| 566 | close | med | — | Decision: ship gstack browse binary in Docker image? | `Dockerfile:136-138` — no gstack install; only Playwright. Option B (skip) is the shipped reality; Phase 7 converged onto /ace:demo pipeline (no in-container browse) | pending gate |
| 666 | implement | high | S | ace_cli_login.py spurious 'live check failed' — expects `{data:{...}}` envelope | `scripts/ace_cli_login.py:103` still unwraps `body.get("data")`; server returns flat `CliAuthUploadOut` (`apps/auth/api.py:224-226`, `apps/auth/schemas.py:64-70`) — warning fires on every successful upload | pending gate |
| 636 | implement | high | M | Runner Nova auth dead — class-level preventers | None of the 4 preventers exist: no seeded-run preflight (`apps/opps/api.py:1615-1775`), no `nova_auth` in `/api/system/version` (`apps/system/api.py:176-191`), refresh failure only logs (`apps/common/nova_auth_flow.py:352`), op-inject failure not surfaced (`docker-entrypoint.sh:100-101`). Helpers exist (`nova_auth_flow.get_fresh_token`/`validate_token`, `apps/auth/api.py:277`) | pending gate |
| 453 | implement | high | M | videos Level 2 post-render QA — per-beat vision judge | `scripts/qa_render.py:18-20` docstring defers to "Level 2 — see the GitHub issue"; no `--vision` flag, no model call anywhere | pending gate |
| 454 | implement | high | M | videos Level 3 post-render QA — transition/boundary check | Only Level 1 exists (`scripts/qa_render.py`); no boundary-frame extraction/perceptual diff/waveform probe. Issue self-deprioritizes ("land only if boundary bugs become real"); vision part depends on #453 | pending gate |
| 516 | implement | high | L | perf(opps): parallelize runs_summary cold-load (53s serial) | Still serial: `apps/opps/api.py:101-133`, `apps/opps/framework_reader.py:176-192`. PR #518 landed then reverted by PR #520 (dropped cards); reopen comment raises bar: partial-error tests, `partial: true` surfacing, rate-limit cap, thread-safety audit, live labs verification | pending gate |

No `investigate` verdicts.

## Recommendations (Phase 4.5)

- **Close #585, #346, #539, #566** with reasoned comments citing the evidence above.
- **#666 — implement now**: one-line client fix (`return body.get("data") or body` or drop the unwrap), branch + draft PR, run pytest/ruff, then ready + auto-merge if green.
- **#636 — implement now (same session, separate PR)**: seeded-run Nova preflight → 409 `nova_auth_invalid`, `nova_auth: {valid, expires_at}` in `/api/system/version`, surface op-inject failure marker. ~2-4h.
- **#453 / #454 — keep open**: valid feature work, but #454 self-deprioritizes and #453 is its prerequisite; not triage-session builds.
- **#516 — keep open**: L-effort with a prior revert and live-verification requirements; needs a dedicated session, not a triage drive-by.
