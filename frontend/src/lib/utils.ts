// cn now lives in the shared design system. Re-exported here so the many
// existing `@/lib/utils` import sites keep working while the implementation
// is sourced from @marshellis/canopy-ui (one cn across ace-web + canopy-web).
export { cn } from "@marshellis/canopy-ui/lib";
